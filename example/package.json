{
  "private": true,
  "dependencies": {
    "next": "^13.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "eslint": "^8.26.0",
    "eslint-config-next": "^13.0.0",
    "next-nginx-routes": "./.."
  },
  "scripts": {
    "export": "next build && next export && next-nginx-routes"
  }
}
